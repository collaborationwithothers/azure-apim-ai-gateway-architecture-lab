name: Validate Markdown

on:
  pull_request:
  workflow_dispatch:

jobs:
  markdown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check whitespace
        run: git diff --check
      - name: Markdown lint placeholder
        run: |
          echo "TODO: Install and run markdownlint once project rules are finalized."
