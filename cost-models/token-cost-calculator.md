# Token Cost Calculator

This placeholder will document how to estimate token cost by tenant, product, model, and cache hit ratio. It intentionally contains no live pricing values.

## TODOs

- Add input fields for tenant, model, prompt tokens, completion tokens, request count, and cache hit ratio.
- Add source citations for pricing values when implemented.
