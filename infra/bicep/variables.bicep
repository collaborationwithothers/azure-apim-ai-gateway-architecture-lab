targetScope = 'resourceGroup'

// Placeholder file for future shared variables.
// Keep environment-specific values in parameter files outside source control.

var requiredFutureServices = [
  'api-management'
  'ai-backend'
  'application-insights'
  'log-analytics'
  'redis-compatible-cache'
  'key-vault'
  'private-endpoints'
  'private-dns'
]

output services array = requiredFutureServices
