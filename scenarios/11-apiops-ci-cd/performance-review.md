# Performance Review: APIOps CI-CD

## Purpose

This page supports the APIOps CI-CD scenario. It captures policy validation, Bicep validation, environment promotion, approvals, drift review, and safe deployment workflow in a format that can be expanded during later implementation.

## Current content

The APIOps CI-CD scenario focuses on policy validation, Bicep validation, environment promotion, approvals, drift review, and safe deployment workflow. The first-pass content is intentionally descriptive and safe. It avoids real endpoints and keeps implementation details as TODOs until the lab has mock services and deployment inputs.

## TODOs

- Replace placeholders with environment-specific values during a controlled deployment pass.
- Add tests against a mock backend before importing policies into APIM.
- Update citations and policy syntax if Microsoft Learn changes relevant APIM AI gateway behavior.
